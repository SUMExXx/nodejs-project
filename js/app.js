const formData = {}

formElement = document.getElementById("form")

function storeData(event){
    event.preventDefault();
    elementsList = document.querySelectorAll(".formStackElements")

    console.log(elementsList)
    // for(i in elementsList){

    // }
}